[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: fast pure-logic tests, no IO or network
    integration: requires an ephemeral Postgres (testcontainers + Docker)
