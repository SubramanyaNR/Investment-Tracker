# Dev/test-only dependencies — NOT installed into the production image.
# Install: .venv/bin/pip install -r requirements-dev.txt
pytest==9.0.3
pytest-asyncio==1.4.0
testcontainers==4.14.2
# httpx + asyncpg come from the prod requirements (reused by the integration tier).
